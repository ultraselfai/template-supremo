"use server";

/**
 * Projects Server Actions
 *
 * Este arquivo contém todas as Server Actions relacionadas a projetos.
 * Seguindo o padrão Feature-Based Architecture:
 * - Validação com Zod antes de qualquer operação
 * - Verificação de tenantId em operações multi-tenant
 * - Tratamento de erros consistente
 */

// TODO: Implementar actions de projetos quando a feature for desenvolvida
// Exemplos:
// - createProject(formData: FormData)
// - updateProject(id: string, formData: FormData)
// - deleteProject(id: string)
// - listProjects(tenantId: string)

export async function placeholder() {
  // Placeholder para manter o arquivo válido
  return { success: true };
}
